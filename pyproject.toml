[tool.ruff]
    line-length = 120

[tool.ruff.format]
    quote-style = 'single'

[tool.ruff.lint]
    extend-select = ['I']

[tool.ruff.lint.isort]
    combine-as-imports = true


[tool.pytest.ini_options]
    asyncio_mode = "auto"

[tool.logfire]
    ignore_no_config = true
